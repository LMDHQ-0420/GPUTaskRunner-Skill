---
name: GPU-task-runner
description: >
  GPU 批量任务调度器：探测当前 GPU 状态，从对话上下文提取任务信息，计算最优并发方案，在已有脚本上追加调度逻辑并输出可直接运行的 shell 脚本。
version: 1.1.0
---

# GPU Task Runner

从对话上下文复用已有任务信息，探测 GPU 实时状态，在已有 `.sh` 脚本上追加调度块，一步生成可直接运行的批量调度脚本。

## 优先级规则

**用户当前指令 > 对话历史信息 > 本 skill 默认行为**

所有参数、路径、运行方式，以用户最新指令为准；对话历史次之；skill 默认值兜底。

## 调用方式

```
/GPU-task-runner
/GPU-task-runner [具体指令]
```

具体指令示例：
- `/GPU-task-runner 只用 GPU 0 和 1`
- `/GPU-task-runner 每个任务限制 8GB 显存`
- `/GPU-task-runner 生成脚本但不运行`

---

## 设计原则

- **轻量复用**：对话中已有的命令、脚本、环境直接使用，不重复收集
- **追加优先**：有已有脚本则追加调度块，无则生成独立 `run_batch.sh`
- **一次性补全**：缺失信息合并成一个问题，不拆多轮
- **纯 bash**：调度层用 shell 实现，`CUDA_VISIBLE_DEVICES` 分配 GPU，`wait` 控制并发
- **日志统一**：每个子任务的 stdout/stderr 重定向到 `logs/YYYY-MM-DD_HH-MM-SS/<任务标识>.log`

---

## 执行流程

### Step 1：解析用户指令

若用户随 skill 附带了具体指令，优先提取其中的约束：
- 指定 GPU 编号 → 仅使用这些卡，不再过滤其他卡
- 指定显存上限 → 覆盖从对话推断的 per_task_vram
- 指定运行方式（直接运行 / 仅生成脚本）→ 覆盖默认行为

### Step 2：探测 GPU 状态

```bash
nvidia-smi --query-gpu=index,utilization.gpu,memory.free,memory.total \
           --format=csv,noheader,nounits
```

解析每张卡的编号、利用率（%）、空闲显存（MiB）、总显存（MiB）。

**可用判定**：利用率 < 80，且空闲显存 > 每任务显存需求（未知时暂不过滤）。

若无 `nvidia-smi`：报错终止，提示检查驱动。

### Step 3：从对话历史提取任务信息

从当前会话历史中查找：

- **运行方式**：对话中是否明确说"直接运行"或"只生成脚本"→ 默认直接运行
- **任务类型**：命令中是否含 `torchrun` / `deepspeed` / `accelerate` / `--nproc` → 单卡 or 多卡
- **多卡数量**：从 `--nproc_per_node` / `--num_gpus` 等参数提取每任务所需卡数
- **每任务显存**：对话中已提及 > 根据模型名 / batch size / 精度估算 > 列入缺失项
- **任务列表**：文件路径（每行一项）/ 数字 N / 参数数组
- **运行命令**：对话中已有的命令，用 `{item}` 标记任务参数占位符
- **已有脚本**：对话中提及的 `.sh` 文件路径
- **Python 环境**：conda 环境名或 venv 路径

### Step 4：缺失信息处理

提取后仍有缺口时，**一次性**输出（只列真正缺失的项）：

```
需要补充以下信息：

1. 每个任务的运行命令？（用 {item} 代表任务参数，例：python eval.py --input {item}）
2. 任务列表？（文件路径 / 数量 N / 参数列表）
3. 每个任务预计占用显存？（GB，不确定可给范围）
```

### Step 5：计算调度方案

**单卡任务：**

```
可用显存(卡i) = 空闲显存(卡i) × 0.85        # 留 15% buffer
并发槽(卡i)   = floor(可用显存(卡i) / 每任务显存)
并发槽(卡i)   = max(1, 并发槽(卡i))          # 可用卡至少 1 个槽
总并发数       = Σ 并发槽(卡i)
```

**多卡任务（每任务需 k 张卡）：**

```
# 空闲卡按显存降序排列，每 k 张连续卡为一组
任务组数 = floor(可用卡数 / k)
总并发数 = 任务组数
```

使用连续 GPU index（如 `0,1,2,3`）保证 NCCL 通信效率。

### Step 6：展示调度摘要

```
━━━━━━━━━━ GPU 调度方案 ━━━━━━━━━━
任务总数：24   并发槽：5   预计轮次：5

GPU  空闲显存   并发槽
 0   18.2 GB    2
 1   22.1 GB    2
 2   10.4 GB    1
 3   [跳过]      ← 利用率 91%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 7：生成调度块，追加到已有脚本

追加内容放在脚本末尾，以注释标记边界。每个子任务的输出重定向到独立日志文件。

**单卡任务模板：**

```bash
# ── GPU Task Runner (generated by /GPU-task-runner) ──────────────
_TASKS=(item0 item1 item2 ...)
_MAX_JOBS=5
_GPU_SLOTS=([0]=2 [1]=2 [2]=1)
_RUNNING=0
_LOG_DIR="logs/$(date +%Y-%m-%d_%H-%M-%S)"
mkdir -p "$_LOG_DIR"

_run_task() {
  local gpu=$1 item=$2
  local log="$_LOG_DIR/${item//\//_}.log"
  CUDA_VISIBLE_DEVICES=$gpu <命令模板，{item} 替换为 "$item"> \
    >"$log" 2>&1
}

for item in "${_TASKS[@]}"; do
  for gpu in "${!_GPU_SLOTS[@]}"; do
    if (( _GPU_SLOTS[$gpu] > 0 )); then
      _run_task "$gpu" "$item" &
      (( _GPU_SLOTS[$gpu]-- ))
      (( _RUNNING++ ))
      break
    fi
  done
  if (( _RUNNING >= _MAX_JOBS )); then
    wait -n 2>/dev/null || wait
    (( _RUNNING-- ))
  fi
done
wait
echo "All tasks done. Logs: $_LOG_DIR"
# ── End GPU Task Runner ───────────────────────────────────────────
```

**多卡任务模板：**

```bash
# ── GPU Task Runner (generated by /GPU-task-runner) ──────────────
_LOG_DIR="logs/$(date +%Y-%m-%d_%H-%M-%S)"
mkdir -p "$_LOG_DIR"

_TASK_GROUPS=(
  "0,1,2,3:item0"
  "4,5,6,7:item1"
)

for entry in "${_TASK_GROUPS[@]}"; do
  gpus="${entry%%:*}"
  item="${entry##*:}"
  log="$_LOG_DIR/${item//\//_}.log"
  CUDA_VISIBLE_DEVICES=$gpus <torchrun/deepspeed 命令，{item} 替换为 "$item"> \
    >"$log" 2>&1 &
done
wait
echo "All tasks done. Logs: $_LOG_DIR"
# ── End GPU Task Runner ───────────────────────────────────────────
```

### Step 8：运行或交付

从对话历史判断运行方式（用户指令优先）：

- **直接运行**（默认）：追加完成后立即执行 `bash <脚本>`，实时输出进度
- **仅生成脚本**：追加完成后输出路径和运行命令，不自动执行

```
已追加调度逻辑到 ./train.sh
运行：bash train.sh
日志目录：logs/YYYY-MM-DD_HH-MM-SS/
```

若无已有脚本，则写入 `./run_batch.sh`。

---

## 边界情况

| 情况 | 处理 |
|------|------|
| 无 `nvidia-smi` | 报错终止，提示检查驱动 |
| 所有卡满载或显存不足 | 输出当前状态，提示稍后重试或减小 batch size |
| 多卡任务空闲卡数不足一组 | 提示当前可用卡不足，建议等待或调整每任务所需卡数 |
| 单卡显存不足 | 跳过该卡，说明原因 |
| 只有 1 张可用卡 | 单槽串行运行，仍生成脚本 |
| 无已有脚本 | 生成独立 `run_batch.sh` |
